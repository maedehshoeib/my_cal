# گزارش تغییرات نسخه 2.0

## 📅 تاریخ: 1404/11/24

## 🎯 هدف
اصلاح و بهبود فرمول‌های محاسبه مالیات علی‌الحساب طبق فایل اکسل رسمی سازمان امور مالیاتی

## ✨ تغییرات اعمال شده

### 1. اصلاح فرمول تعدیل با ضریب تبدیل

#### ❌ قبل:
```javascript
مقدار تعدیلی = مقدار اصلی × ضریب تبدیل
```

#### ✅ بعد:
```javascript
مقدار تعدیلی = مقدار اصلی + (ضریب تبدیل × مقدار اصلی)
// یا به عبارت دیگر:
مقدار تعدیلی = مقدار اصلی × (1 + ضریب تبدیل)
```

**دلیل:** طبق فایل اکسل (سلول H6): `=E6+(G6*E6)` که نشان می‌دهد ضریب باید به مقدار اصلی **اضافه** شود.

### 2. تغییر روش پیش‌بینی از رشد متوسط به رگرسیون خطی

#### ❌ قبل:
```javascript
نرخ رشد = میانگین درصد افزایش
تخمین سال جاری = سال قبل × (1 + نرخ رشد)
```

#### ✅ بعد:
```javascript
y = bx + y₀

b = [(سال2 - سال1) + (سال3 - سال2)] ÷ 2
y₀ = سال1 - b
پیش‌بینی سال 4 = (4 × b) + y₀
```

**دلیل:** طبق فایل اکسل (ردیف‌های 12-16) فرمول رگرسیون خطی استفاده شده است.

### 3. اصلاح فرمول محاسبه تخفیف

#### ❌ قبل:
```javascript
ضریب وفاداری = نمره کل ÷ 25
تخفیف = ضریب وفاداری × حداکثر تخفیف
```

#### ✅ بعد:
```javascript
تخفیف = (نمره کل ÷ 25) × حداکثر تخفیف
```

**دلیل:** طبق شیت دوم (سلول F35): `=(F34/F33)*F32` که نشان می‌دهد فرمول دقیقاً همین است. البته نتیجه یکسان است اما برای شفافیت بیشتر اصلاح شد.

### 4. اصلاح مقدار پیش‌فرض ضریب تبدیل

#### ❌ قبل:
```
placeholder="1.0"
```

#### ✅ بعد:
```
placeholder="0.15"
```

**دلیل:** طبق فایل اکسل، ضرایب تبدیل معمولاً اعدادی مانند 0.15، 0.20، 0.22 هستند.

### 5. اصلاح مقدار پیش‌فرض حداکثر تخفیف

#### ❌ قبل:
```javascript
const maxDiscountPercent = parseNumber(maxDiscount) || 0;
placeholder="0"
```

#### ✅ بعد:
```javascript
const maxDiscountPercent = parseNumber(maxDiscount) || 50;
placeholder="50"
```

**دلیل:** طبق شیت دوم (سلول F32): "ماکزیمم درصد تخفیف برای مودیان نمونه = 50"

### 6. افزودن محاسبه و نمایش میانگین هم‌وزن شده

```javascript
میانگین = (مقدار سال1 + مقدار سال2 + مقدار سال3) ÷ 3
```

**دلیل:** طبق فایل اکسل (سلول T6): `=(H6+M6+R6)/3`

### 7. نمایش ضرایب رگرسیون (b و y₀)

اکنون در نتایج، ضرایب رگرسیون برای هر شاخص نمایش داده می‌شود:
```
b=13.95, y0=43.55
```

## 📝 فایل‌های تغییر یافته

### 1. `/frontend/src/components/LoyaltyTaxCalculator.jsx`
- اصلاح فرمول تعدیل (خط ~160)
- تغییر از رشد متوسط به رگرسیون خطی (خط ~180-260)
- اصلاح فرمول تخفیف (خط ~290)
- بهبود نمایش نتایج (خط ~790-850)

### 2. `/frontend/src/components/LoyaltyTaxCalculator.css`
- افزودن استایل‌های جدید برای نمایش رگرسیون
- استایل `.averages-section` و `.regression-section`
- استایل `.mini-title`
- استایل `.regression-info` و `.result-calculation`

### 3. فایل‌های مستندات جدید:

#### `/FORMULAS.md`
- مستندات کامل تمام فرمول‌ها
- توضیح گام‌به‌گام هر محاسبه
- مثال‌های متعدد
- 6 بخش اصلی

#### `/EXAMPLE.md`
- یک مثال کامل با داده‌های واقعی از فایل اکسل
- محاسبات گام‌به‌گام
- جداول خلاصه نتایج
- نکات مهم

#### `/README.md`
- به‌روزرسانی کامل
- توضیح ویژگی‌ها
- راهنمای استفاده
- لینک به مستندات

## 🔍 تست‌های انجام شده

✅ کامپایل موفق بدون هیچ خطایی
✅ تطابق کامل با فرمول‌های فایل اکسل
✅ محاسبات رگرسیون صحیح
✅ نمایش صحیح نتایج

## 📊 مقایسه نتایج

### داده‌های تست (از فایل اکسل):

**ورودی:**
- سال 3: فروش ابرازی=100، ضریب=0.15
- سال 2: فروش ابرازی=120، ضریب=0.20
- سال 1: فروش ابرازی=145، ضریب=0.22

**نتایج تعدیل:**
- سال 3: 115
- سال 2: 144
- سال 1: 176.9

**رگرسیون:**
- b ≈ 30.95
- y₀ ≈ 84.05
- پیش‌بینی ≈ 207.85

این نتایج با فایل اکسل مطابقت دارد! ✅

## 🎯 نتیجه‌گیری

تمام فرمول‌های سیستم اکنون **100% منطبق** بر فایل اکسل رسمی است:

1. ✅ تعدیل با ضریب تبدیل
2. ✅ میانگین هم‌وزن شده
3. ✅ رگرسیون خطی برای پیش‌بینی
4. ✅ محاسبه مالیات (25%)
5. ✅ محاسبه تخفیف وفاداری
6. ✅ محاسبه مالیات نهایی

## 📚 منابع

- **فایل اکسل مرجع:**  
  `/home/maede/Downloads/BA 0905-الگوریتم و مبانی تعیین علی الحساب مالیاتی2.xlsx`
  
- **شیت اول:** "جدول محاسبه علی الحساب مالیات"
  - ردیف‌های 6-9: داده‌های نمونه
  - ردیف‌های 12-16: فرمول‌های رگرسیون
  
- **شیت دوم:** "محاسبه امتیاز رفتار مالیاتی"
  - ردیف 32: ماکزیمم درصد تخفیف = 50%
  - ردیف 33: ماکزیمم ضرایب = 25
  - ردیف 35: فرمول محاسبه تخفیف

## 👨‍💻 توسعه‌دهنده

تغییرات توسط GitHub Copilot اعمال شد.

---

**تاریخ:** 1404/11/24  
**نسخه:** 2.0  
**وضعیت:** ✅ تأیید شده و آماده استفاده
